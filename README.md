# Hiyori App

A language learning app with flashcards, daily journal, and an AI tutor, that can be self hosted (AI Tutor and Insights will only be added in a future update).

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
